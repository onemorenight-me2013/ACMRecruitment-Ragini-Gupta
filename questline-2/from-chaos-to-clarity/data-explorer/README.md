for displaying the dataset we will use "read_csv(<file_name>)". csv because the file given to us is in csv form , it will read it and print the data it has read for shape 
we use"df.shape". this describes the shape , whether it is in tabular form or some other form of representation 
"df.columns" is used to describe the columns of the dataset 
df.types- is used to get the datatypes stored in each columns 
df.isnull().sum()- checks through out the data whether the given value is null and if any , it sums it up 
df.describe - describing the dataset
